name: mechaniq
description: A new Flutter project.
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5
  permission_handler: ^11.4.0
  http: ^1.2.0
  sqflite: ^2.3.0
  path: ^1.8.3
  google_fonts: ^6.1.0
  flutter_spinkit: ^5.2.2
  # नए असली फीचर्स के पैकेजेस (Firebase, Bluetooth, AI) 👇
  firebase_core: ^2.32.0
  firebase_auth: ^4.15.3
  cloud_firestore: ^5.0.1
  flutter_blue_plus: ^1.31.0
  google_generative_ai: ^0.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
